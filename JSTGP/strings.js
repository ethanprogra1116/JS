"use strict";
// Todos los strings son de 16 bits o 2 bytes cuando UNICODE solo era de ese length

let largo_emoji = "😍".length;
let largo_ascii = "a".length;
let numero_ascii = "a".charCodeAt();
console.log("El largo de un emoji es de " + largo_emoji);
console.log('El numero ascii de "a" es de ' + numero_ascii);
